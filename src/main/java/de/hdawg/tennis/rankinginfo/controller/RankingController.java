package de.hdawg.tennis.rankinginfo.controller;import de.hdawg.tennis.rankinginfo.model.Ranking;import de.hdawg.tennis.rankinginfo.repository.RankingRepository;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.PostMapping;import org.springframework.web.bind.annotation.RequestParam;import java.util.List;@Slf4j@RequiredArgsConstructor@Controllerpublic class RankingController {    private final RankingRepository rankingRepository;    @GetMapping("/listings")    public String getRankingListStartPage(Model model) {        return "listing/listingStart";    }    @PostMapping("/listings")    public String getRankingListing(@RequestParam(name = "quarter", required = false) String period, @RequestParam(name = "gender", required = false) String gender, @RequestParam(name = "age_group", required = false) String ageGroup, @RequestParam(name = "age_group_options", required = false) String ageGroupOptions, @RequestParam(name = "federation", required = false) String federation, @RequestParam(name = "club", required = false) String club, @RequestParam(name = "commit", required = false) String commited, Model model) {        // TODO prepare ranking age group and period to be correctly sent        // period and year-end-marker        // age group + modifier        // federation if present        // club if present        // is there an automatic mechanism to avoid sql injection?        log.debug("search triggered - processing search params");        List<Ranking> rankings = rankingRepository.retrieveRankingListItems("", false, "U14c", null, "BVH Tennis");        model.addAttribute(rankings);        return "listing/listingResult";    }}